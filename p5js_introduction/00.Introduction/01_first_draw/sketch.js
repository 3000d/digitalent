/*
* la fonction setup est celle dans laquelle nous allons
* définir les paramètres de départ de notre sketch
*/
function setup() {
  // dessiner un cercle dans p5js est aussi simple que cela
  ellipse(50,50,10,10);

  // pour dessiner un carré ou un rectangle
  rect(70,80, 10,10);

}
